# SEAD 3 Compliance Auditor -- runtime dependencies
#
# These use minimum-version floors (>=) rather than exact pins so that pip
# always grabs a build that has a ready-made wheel for your Python version --
# including brand-new Pythons like 3.14. This avoids pip trying to COMPILE
# packages from source (which would require a C/Rust toolchain you likely
# don't have). pydantic>=2.11 is the key line: older pydantic had no prebuilt
# package for Python 3.14 and tried to build itself, which fails without a
# compiler. The newer one installs instantly with no compiler needed.

fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0.36
jinja2>=3.1.4
python-multipart>=0.0.20
reportlab>=4.2.5
pydantic>=2.11
